import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(
      home: Scaffold(
          appBar: AppBar(
            title: const Text('Reyjager Coffee'),
            backgroundColor: Colors.brown[700],
            centerTitle: true,
          ),
          body: const Text('Zip your Coffee Now'))));
}
